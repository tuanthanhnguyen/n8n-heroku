services:
  - type: web
    name: n8n
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: n8n-posgres
          property: connectionString

databases:
  - name: n8n-posgres
    plan: free
